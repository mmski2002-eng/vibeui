import type { ReactNode } from "react"

/** Один кадр прогона: компонент на случайных настройках. */
export type LabCheck = {
  name: string
  title: string
  /** Что выпало на этот прогон — подпись под кадром. */
  notes: string[]
  node: ReactNode
  /**
   * Отдельный экземпляр для тёмного кадра. Нужен компонентам, которые
   * группируют разделы атрибутом name: две копии с одним именем делят
   * радиогруппу, и второй кадр стоит закрытым.
   */
  nodeDark?: ReactNode
}
