/**
 * Месячные лимиты копирования.
 *
 * Отдельным файлом от `entitlements.ts`: тот помечен "server-only" и тянет
 * за собой базу, а числа нужны и статическим страницам — лендингу и
 * тарифам. Импортировать ради двух констант серверный модуль с драйвером
 * postgres значило бы уронить генерацию этих страниц.
 */

/** Сколько разных компонентов в месяц отдаём бесплатно вошедшему. */
export const FREE_MONTHLY_LIMIT = 20
