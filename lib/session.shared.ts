/**
 * Имя заголовка с текущим адресом. Лежит отдельно от `session.ts`, потому
 * что его ставит proxy.ts, а тот работает в edge-окружении и не может
 * тянуть за собой серверный модуль с базой и куками.
 */
export const PATH_HEADER = "x-vibeui-path"
