{
  "$schema": "https://ui.shadcn.com/schema/registry.json",
  "items": [
    {
      "name": "pagination-001",
      "type": "registry:component",
      "title": "Постраничная навигация ссылками",
      "description": "Постраничная навигация обычными ссылками: страницу можно открыть в новой вкладке, скопировать и найти в истории. Окно номеров вокруг текущей.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-001/pagination-001.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-001.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-001-*. Номера — обычные <a>, адрес собирает функция hrefOf, по умолчанию `?page=N`. Окно вокруг текущей страницы считает windowOf: первая, последняя, соседи и разрывы между ними. Текущая страница помечена aria-current=\"page\". Края не исчезают на первой и последней странице, а гасятся через aria-disabled, поэтому ряд не прыгает.",
      "meta": {
        "tags": ["pagination", "navigation", "links", "list"],
        "ai": {
          "export": "Pagination001",
          "usage": "<Pagination001\n  page={4}\n  total={12}\n  hrefOf={(page) => `/blog?page=${page}`}\n/>",
          "summary": "Постраничная навигация ссылками: стрелки по краям, окно номеров вокруг текущей страницы и разрывы вместо длинного списка. Ширина ряда не скачет при переходе, потому что края остаются на месте. Ноль зависимостей, один файл, своя палитра, клиентского JS нет.",
          "preserve": [
            "локальную палитру --vibeui-pagination-001-* и не подменять её токенами вашей темы (bg-primary, border-input и подобными)",
            "номера как ссылки, а не кнопки: открыть в новой вкладке, скопировать адрес и вернуться из истории — всё это идёт бесплатно",
            "aria-current=\"page\" у текущей страницы: без него активный номер отличается только цветом",
            "края, которые гасятся, а не исчезают: пропадающая стрелка сдвигает весь ряд",
            "tabular-nums у номеров — без них ширина кнопок пляшет от цифр",
            "подписи aria-label у стрелок: «←» скринридер прочитает как символ",
            "блок <style> внутри компонента — в нём вся палитра и раскладка",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему"
          ],
          "adapt": [
            "page и total — текущая страница и общее количество",
            "siblings — сколько номеров показывать вокруг текущей",
            "hrefOf — как собрать адрес страницы под ваш роутер",
            "акцент через проп accent — цвет активного номера",
            "внешние отступы через className",
            "подписи через labelText — заголовок навигации и названия стрелок; по умолчанию русские"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Для бесконечной ленты пагинация не нужна: это разные способы чтения списка.",
            "Не заменяйте ссылки на onClick без адреса: тогда страницу нельзя открыть в новой вкладке и она пропадёт из истории.",
            "Общее количество страниц должно быть известно. Если его нет, показывайте «дальше» без номеров.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "page",
            "label": "Страница",
            "type": "number",
            "default": 4,
            "min": 1,
            "max": 12
          },
          {
            "prop": "total",
            "label": "Всего",
            "type": "number",
            "default": 12,
            "min": 1,
            "max": 99
          },
          {
            "prop": "siblings",
            "label": "Соседей",
            "type": "number",
            "default": 1,
            "min": 0,
            "max": 3
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "Pagination made of ordinary links: a page can be opened in a new tab, copied and found in history. A window of numbers around the current page.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-001-* variables. The numbers are ordinary <a> elements, and the address is assembled by the hrefOf function, `?page=N` by default. The window around the current page is computed by windowOf: the first page, the last one, the neighbours and the gaps between them. The current page is marked with aria-current=\"page\". The edges do not disappear on the first and the last page but dim through aria-disabled, so the row does not jump.",
            "ai": {
              "summary": "Link-based pagination: edge arrows, a window of numbers around the current page and gaps instead of a long list. The row does not jump between pages because the edges stay in place. Zero dependencies, one file, its own palette, no client JS.",
              "preserve": [
                "the local --vibeui-pagination-001-* palette — do not swap it for your theme tokens (bg-primary, border-input and the like)",
                "the numbers as links rather than buttons: opening in a new tab, copying the address and going back through history all come free",
                "aria-current=\"page\" on the active number: without it the active page differs only by colour",
                "edges that dim instead of disappearing: a vanishing arrow shifts the whole row",
                "tabular-nums on the numbers — without them the buttons jitter as digits change",
                "the aria-labels on the arrows: a screen reader would otherwise announce \"←\" as a symbol",
                "the <style> block inside the component — it holds the palette and the layout",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own"
              ],
              "adapt": [
                "page and total — the current page and the count",
                "siblings — how many numbers surround the current page",
                "hrefOf — how to build a page address for your router",
                "the accent through the accent prop — it colours the active number",
                "outer spacing through className",
                "the labels through labelText — the nav title and the arrow names; Russian by default"
              ],
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "An infinite feed needs no pagination: they are two different ways of reading a list.",
                "Do not replace the links with onClick and no href: the page then cannot be opened in a new tab and vanishes from history.",
                "The total has to be known. Without it, show a plain \"next\" without numbers.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ],
              "usage": "<Pagination001\n  page={4}\n  total={12}\n  hrefOf={(page) => `/blog?page=${page}`}\n/>"
            },
            "controls": {
              "page": {
                "label": "Page"
              },
              "total": {
                "label": "Total"
              },
              "siblings": {
                "label": "Siblings"
              },
              "accent": {
                "label": "Accent"
              }
            },
            "title": "Link Pagination",
            "preview": {
              "props": {
                "labelText": {
                  "nav": "Pages",
                  "prev": "Previous page",
                  "next": "Next page"
                }
              }
            }
          }
        },
        "cardControls": ["page", "siblings", "total", "accent"]
      }
    },
    {
      "name": "pagination-002",
      "type": "registry:component",
      "title": "Навигация курсором",
      "description": "Курсорная навигация: диапазон строк вместо номеров страниц — он остаётся верным, когда данные меняются на ходу.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-002/pagination-002.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-002.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-002-*. Серверный: хуков нет, границы считаются из пропсов. Край списка помечен aria-disabled на ссылке: кнопка остаётся на месте, но не работает, поэтому строка навигации не прыгает. Числа набраны табличными цифрами.",
      "meta": {
        "tags": ["pagination", "cursor", "list", "range"],
        "preview": {
          "width": "full"
        },
        "ai": {
          "export": "Pagination002",
          "usage": "<Pagination002 from={41} perPage={20} total={214} />",
          "summary": "Курсорная навигация: диапазон строк слева, кнопки «Назад» и «Вперёд» справа, край списка приглушён. Ноль зависимостей, один файл, клиентского JS нет.",
          "preserve": [
            "локальную палитру --vibeui-pagination-002-* и не подменять её токенами вашей темы",
            "счёт по строкам, а не по страницам: при меняющихся данных номер страницы врёт",
            "aria-disabled на краю списка вместо удаления кнопки — иначе строка прыгает",
            "табличные цифры в диапазоне",
            "ссылки, а не кнопки: адрес страницы должен открываться в новой вкладке",
            "стрелки из бордюров: они поворачиваются вместе с направлением",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку"
          ],
          "adapt": [
            "from, perPage и total",
            "prevHref и nextHref",
            "акцент через проп accent",
            "ширину строки",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "подписи через labelText и строку счёта rangeText с {range} и {total}"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Компонент только показывает границы: курсор и запрос держит вызывающий код.",
            "Номеров страниц нет намеренно: для стабильных данных с переходом на страницу берите pagination-001.",
            "pointer-events:none на краю не отменяет переход по клавиатуре — уберите href, если нужен полный запрет.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "from",
            "label": "Первая строка",
            "type": "number",
            "default": 41,
            "min": 1,
            "max": 200
          },
          {
            "prop": "perPage",
            "label": "На странице",
            "type": "number",
            "default": 20,
            "min": 5,
            "max": 50
          },
          {
            "prop": "total",
            "label": "Всего",
            "type": "number",
            "default": 214,
            "min": 10,
            "max": 999
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "Cursor pagination: a row range instead of page numbers — it stays true while the data keeps changing.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-002-* variables. Server rendered: no hooks, the bounds are computed from the props. The edge of the list is marked with aria-disabled on the link: the button stays in place but does not work, so the navigation row does not jump. The numbers are set in tabular figures.",
            "ai": {
              "summary": "Cursor pagination: the row range on the left, Previous and Next on the right, muted at the edge of the list. Zero dependencies, one file, no client JS.",
              "preserve": [
                "the local --vibeui-pagination-002-* palette — do not swap it for your theme tokens",
                "counting rows rather than pages: with changing data a page number lies",
                "aria-disabled at the edge instead of removing the button — otherwise the row jumps",
                "tabular figures in the range",
                "links rather than buttons: the page address must open in a new tab",
                "the border-drawn arrows: they rotate with the direction",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself"
              ],
              "adapt": [
                "from, perPage and total",
                "prevHref and nextHref",
                "the accent through the accent prop",
                "the row width",
                "background — the component background; empty means no background, taking the page colour",
                "the labels through labelText and the counter string rangeText with {range} and {total}"
              ],
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The component only shows the bounds: the cursor and the request stay in the caller.",
                "Page numbers are deliberately absent: for stable data with jump-to-page use pagination-001.",
                "pointer-events:none at the edge does not block keyboard activation — drop the href for a hard stop.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ],
              "usage": "<Pagination002 from={41} perPage={20} total={214} />"
            },
            "controls": {
              "from": {
                "label": "First row"
              },
              "perPage": {
                "label": "Per page"
              },
              "total": {
                "label": "Total"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Cursor Pagination",
            "preview": {
              "props": {
                "labelText": {
                  "nav": "List navigation",
                  "prev": "Back",
                  "next": "Forward"
                },
                "rangeText": "Rows {range} of {total}"
              }
            }
          }
        },
        "cardControls": ["from", "perPage", "total", "background"]
      }
    },
    {
      "name": "pagination-003",
      "type": "registry:component",
      "title": "Многоточие с переходом",
      "description": "Пагинация со свёрткой середины, где «…» не подпись, а кнопка: она перепрыгивает через пропущенный кусок сразу на пять страниц.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-003/pagination-003.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-003.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-003-*. Клиентский: \"use client\" ради состояния текущей страницы. Окно номеров считает функция windowOf: первая, последняя, соседи и разрывы между ними; разрыв помечен нулём, и на его месте рисуется кнопка прыжка. Многоточие превращается в стрелку по :hover и :focus-visible. Текущая страница помечена aria-current=\"page\", числа набраны табличными цифрами.",
      "meta": {
        "tags": ["pagination", "ellipsis", "jump", "list"],
        "preview": {
          "width": "full"
        },
        "ai": {
          "export": "Pagination003",
          "usage": "<Pagination003 total={24} defaultPage={7} siblings={1} />",
          "summary": "Пагинация с окном номеров вокруг текущей страницы и свёрткой середины. Многоточие — живая кнопка: наведение или фокус превращают его в стрелку, а нажатие переносит сразу на пять страниц. Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-003-* и не подменять её токенами вашей темы (bg-primary, border-input и подобными)",
            "многоточие кнопкой, а не подписью: обычный разрыв — тупик, из него до середины списка идут по одной странице",
            "aria-label у кнопки прыжка: «…» скринридер прочитает как символ и не объяснит, куда она ведёт",
            "aria-current=\"page\" у текущей страницы: без него активный номер отличается только цветом",
            "заливку активного номера вместе с насыщенностью — один цвет теряется рядом с наведением",
            "табличные цифры у номеров: без них ширина кнопок пляшет от цифр",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку"
          ],
          "adapt": [
            "total и defaultPage — количество страниц и та, что открыта изначально",
            "siblings — сколько номеров показывать вокруг текущей",
            "величину прыжка многоточия (константа JUMP)",
            "акцент через проп accent — цвет активного номера и колец фокуса",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "подписи через labelText — стрелки, aria-подписи номеров и кнопки прыжка"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Номера здесь кнопки, а не ссылки: страница не открывается в новой вкладке. Нужен адрес — берите вариант на ссылках.",
            "Состояние живёт внутри компонента: чтобы поднять его наверх, замените useState на пропсы со своим обработчиком.",
            "Прыжок на пять страниц подобран под окно в одну соседнюю: при большем siblings увеличьте и его.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "defaultPage",
            "label": "Страница",
            "type": "number",
            "default": 7,
            "min": 1,
            "max": 24
          },
          {
            "prop": "total",
            "label": "Всего страниц",
            "type": "number",
            "default": 24,
            "min": 1,
            "max": 99
          },
          {
            "prop": "siblings",
            "label": "Соседей",
            "type": "number",
            "default": 1,
            "min": 0,
            "max": 3
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "Pagination with a collapsed middle where the ellipsis is not a label but a button: it jumps five pages across the gap at once.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-003-* variables. Client component: \"use client\" for the sake of the current page state. The window of numbers is computed by the windowOf function: the first page, the last one, the neighbours and the gaps between them; a gap is marked with a zero, and a jump button is drawn in its place. The ellipsis turns into an arrow on :hover and :focus-visible. The current page is marked with aria-current=\"page\", and the numbers are set in tabular figures.",
            "ai": {
              "summary": "Pagination with a window of numbers around the current page and a collapsed middle. The ellipsis is a live button: hover or focus turns it into an arrow, and pressing it moves five pages at a time. Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-003-* palette — do not swap it for your theme tokens (bg-primary, border-input and the like)",
                "the ellipsis as a button rather than a label: an ordinary gap is a dead end, and the middle is then reached one page at a time",
                "the aria-label on the jump button: a screen reader announces \"…\" as a symbol and explains nothing",
                "aria-current=\"page\" on the active page: without it the current number differs only by colour",
                "the filled active number together with the weight — colour alone is lost next to a hover state",
                "tabular figures on the numbers: without them the buttons jitter as digits change",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself"
              ],
              "adapt": [
                "total and defaultPage — the page count and the one open at the start",
                "siblings — how many numbers surround the current page",
                "the jump distance of the ellipsis (the JUMP constant)",
                "the accent through the accent prop — it colours the active number and the focus rings",
                "background — the component background; empty means no background, taking the page colour",
                "the labels through labelText — the arrows, the number aria-labels and the jump button"
              ],
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The numbers here are buttons, not links: a page cannot be opened in a new tab. If you need URLs, take the link-based variant.",
                "The state lives inside the component: to lift it up, replace useState with props and your own handler.",
                "The five-page jump is tuned to a window of one sibling: raise it if you raise siblings.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ],
              "usage": "<Pagination003 total={24} defaultPage={7} siblings={1} />"
            },
            "controls": {
              "defaultPage": {
                "label": "Page"
              },
              "total": {
                "label": "Total pages"
              },
              "siblings": {
                "label": "Siblings"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Jumping Ellipsis",
            "preview": {
              "props": {
                "labelText": {
                  "nav": "Result pages",
                  "prev": "Previous page",
                  "next": "Next page",
                  "prevText": "← Back",
                  "nextText": "Forward →",
                  "jumpBack": "Back {count} pages",
                  "jumpForward": "Forward {count} pages",
                  "page": "Page {page}"
                }
              }
            }
          }
        },
        "cardControls": [
          "defaultPage",
          "siblings",
          "total",
          "accent",
          "background"
        ]
      }
    },
    {
      "name": "pagination-004",
      "type": "registry:component",
      "title": "Назад-вперёд со счётчиком",
      "description": "Самая скромная пагинация: две ссылки и счётчик между ними. Номеров нет намеренно — их незачем показывать в ленте, которую читают подряд.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-004/pagination-004.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-004.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-004-*. Серверный: хуков нет, границы считаются из пропсов page и total. Адрес соседней страницы собирает функция hrefOf, по умолчанию `?page=N`. Край списка помечен aria-disabled и гасится, а не исчезает, поэтому ряд не прыгает. Счётчик занимает всё свободное место и набран табличными цифрами, стрелки нарисованы бордюрами и поворачиваются по data-dir.",
      "meta": {
        "tags": ["pagination", "prev-next", "links", "minimal"],
        "ai": {
          "export": "Pagination004",
          "usage": "<Pagination004 page={4} total={12} hrefOf={(page) => `/blog?page=${page}`} />",
          "summary": "Пагинация из двух ссылок и счётчика посередине: «Страница 4 из 12». Края гасятся, а не пропадают, поэтому ширина ряда не меняется при переходе. Ноль зависимостей, один файл, клиентского JS нет.",
          "preserve": [
            "локальную палитру --vibeui-pagination-004-* и не подменять её токенами вашей темы (bg-secondary, border-input и подобными)",
            "ссылки вместо кнопок: открыть в новой вкладке, скопировать адрес и вернуться из истории — всё это идёт бесплатно",
            "rel=\"prev\" и rel=\"next\" на краях: это подсказка и браузеру, и поисковому роботу о порядке страниц",
            "гашение края через aria-disabled вместо удаления ссылки — исчезнувшая ссылка сдвигает счётчик",
            "табличные цифры в счётчике: иначе ссылки по краям дёргаются при смене номера",
            "счётчик текстом, а не только номером: «4 из 12» отвечает на вопрос «сколько осталось», а голая четвёрка — нет",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку"
          ],
          "adapt": [
            "page и total — текущая страница и общее количество",
            "hrefOf — как собрать адрес страницы под ваш роутер",
            "label — слово перед номером",
            "акцент через проп accent — цвет колец фокуса",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "countText — строка счётчика с подстановками {label}, {page} и {total}",
            "подписи стрелок и заголовок навигации через labelText"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Прыгнуть на произвольную страницу отсюда нельзя: это осознанный отказ, для прыжков берите вариант с номерами.",
            "pointer-events:none на краю не отменяет переход по клавиатуре — href там уже не задан, и это главная защита.",
            "Общее количество должно быть известно: для меняющихся данных лучше подходит курсорная навигация.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "page",
            "label": "Страница",
            "type": "number",
            "default": 4,
            "min": 1,
            "max": 12
          },
          {
            "prop": "total",
            "label": "Всего",
            "type": "number",
            "default": 12,
            "min": 1,
            "max": 99
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "The most modest pagination: two links and a counter between them. The numbers are deliberately gone — a feed read in order does not need them.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-004-* variables. Server rendered: no hooks, the bounds are computed from the page and total props. The address of the neighbouring page is assembled by the hrefOf function, `?page=N` by default. The edge of the list is marked with aria-disabled and dims rather than disappears, so the row does not jump. The counter takes all the free space and is set in tabular figures, while the arrows are drawn with borders and rotate according to data-dir.",
            "ai": {
              "summary": "Pagination made of two links and a counter in the middle: \"Page 4 of 12\". The edges dim instead of disappearing, so the row keeps its width between pages. Zero dependencies, one file, no client JS.",
              "preserve": [
                "the local --vibeui-pagination-004-* palette — do not swap it for your theme tokens (bg-secondary, border-input and the like)",
                "links rather than buttons: opening in a new tab, copying the address and going back through history all come free",
                "rel=\"prev\" and rel=\"next\" on the edges: they tell the browser and the crawler how the pages are ordered",
                "dimming the edge with aria-disabled instead of removing the link — a vanished link shifts the counter",
                "tabular figures in the counter: otherwise the edge links jitter as the number changes",
                "the counter as a sentence rather than a bare number: \"4 of 12\" answers \"how much is left\", a lone 4 does not",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself"
              ],
              "adapt": [
                "page and total — the current page and the count",
                "hrefOf — how to build a page address for your router",
                "label — the word before the number",
                "the accent through the accent prop — it colours the focus rings",
                "background — the component background; empty means no background, taking the page colour",
                "countText — the counter string with the {label}, {page} and {total} placeholders",
                "the arrow captions and the nav title through labelText"
              ],
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "There is no way to jump to an arbitrary page: that is a deliberate omission, use the numbered variant for jumps.",
                "pointer-events:none at the edge does not block keyboard activation — the href is already absent there, and that is the real guard.",
                "The total has to be known: for shifting data cursor pagination fits better.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ],
              "usage": "<Pagination004 page={4} total={12} hrefOf={(page) => `/blog?page=${page}`} />"
            },
            "controls": {
              "page": {
                "label": "Page"
              },
              "total": {
                "label": "Total"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Prev Next Counter",
            "preview": {
              "props": {
                "label": "Page",
                "countText": "{label} {page} of {total}",
                "labelText": {
                  "nav": "Page navigation",
                  "prev": "Back",
                  "next": "Forward"
                }
              }
            }
          }
        },
        "cardControls": ["page", "total", "background"]
      }
    },
    {
      "name": "pagination-005",
      "type": "registry:component",
      "title": "«Показать ещё» с прогрессом",
      "description": "«Показать ещё» вместо страниц: полоса прогресса и счётчик «12 из 84» отвечают на главный вопрос длинного списка — сколько осталось.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-005/pagination-005.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-005.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-005-*. Клиентский: \"use client\" ради счётчика показанного. Ширина заливки приходит переменной --vibeui-pagination-005-progress, посчитанной из отношения показанного к общему. Полоса помечена role=\"progressbar\" с aria-valuenow, aria-valuemin и aria-valuemax, а сообщение о догрузке живёт в области aria-live=\"polite\". Когда показано всё, кнопка заменяется строкой о конце списка.",
      "meta": {
        "tags": ["pagination", "load more", "progress", "list"],
        "ai": {
          "export": "Pagination005",
          "usage": "<Pagination005 total={84} perPage={12} label=\"Показать ещё {count}\" />",
          "summary": "Кнопка «Показать ещё» с честной шкалой: полоса прогресса, счётчик показанного и подпись, сколько добавит следующее нажатие. Дойдя до конца, кнопка уступает место строке о том, что это всё. Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-005-* и не подменять её токенами вашей темы (bg-primary, bg-muted и подобными)",
            "счётчик «показано из общего»: без него длинный список превращается в бесконечность без края",
            "role=\"progressbar\" с aria-valuenow и границами: полоса без ролей для скринридера просто цветной прямоугольник",
            "aria-live=\"polite\" у счётчика — иначе догрузка проходит мимо тех, кто не видит экран",
            "число в подписи кнопки: человек должен знать, сколько добавит нажатие",
            "явную строку об окончании списка вместо исчезающей кнопки: пропавшая кнопка читается как поломка",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку"
          ],
          "adapt": [
            "total и perPage — общее количество и размер порции",
            "defaultShown — сколько показано изначально",
            "label — подпись кнопки с подстановкой {count}",
            "акцент через проп accent — цвет полосы прогресса",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "countText — строка счётчика с подстановками {shown} и {total}",
            "подписи через labelText — aria-подпись полосы и строка об окончании списка"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Компонент показывает только счётчики: сам список и загрузку данных держит вызывающий код.",
            "Позиция прокрутки после догрузки не трогается — это верно для «показать ещё», но неверно для замены страницы.",
            "Такой список плохо возвращается по ссылке: место в нём нельзя передать адресом. Нужен адрес — берите страницы.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "total",
            "label": "Всего",
            "type": "number",
            "default": 84,
            "min": 1,
            "max": 999
          },
          {
            "prop": "perPage",
            "label": "Порция",
            "type": "number",
            "default": 12,
            "min": 1,
            "max": 100
          },
          {
            "prop": "defaultShown",
            "label": "Показано",
            "type": "number",
            "default": 12,
            "min": 0,
            "max": 84
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "\"Load more\" instead of pages: a progress bar and a \"12 of 84\" counter answer the one question a long list raises — how much is left.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-005-* variables. Client component: \"use client\" for the sake of the counter of what is shown. The fill width arrives in the --vibeui-pagination-005-progress variable, computed from the ratio of what is shown to the total. The bar is marked role=\"progressbar\" with aria-valuenow, aria-valuemin and aria-valuemax, and the message about the load lives in an aria-live=\"polite\" region. Once everything is shown, the button is replaced by a line about the end of the list.",
            "ai": {
              "summary": "A \"Load more\" button with an honest scale: a progress bar, a counter of what is shown and a label saying how much the next press adds. At the end the button gives way to a line saying that is all. Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-005-* palette — do not swap it for your theme tokens (bg-primary, bg-muted and the like)",
                "the \"shown of total\" counter: without it a long list becomes an edgeless infinity",
                "role=\"progressbar\" with aria-valuenow and the bounds: without roles the bar is just a coloured rectangle to a screen reader",
                "aria-live=\"polite\" on the counter — otherwise the load goes unnoticed by anyone not watching the screen",
                "the number in the button caption: people have to know how much a press adds",
                "the explicit end-of-list line instead of a vanishing button: a disappeared button reads as a breakage",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself"
              ],
              "adapt": [
                "total and perPage — the overall count and the batch size",
                "defaultShown — how much is shown at the start",
                "label — the button caption with the {count} placeholder",
                "the accent through the accent prop — it colours the progress bar",
                "background — the component background; empty means no background, taking the page colour",
                "countText — the counter string with the {shown} and {total} placeholders",
                "the labels through labelText — the bar aria-label and the end-of-list line"
              ],
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The component only shows the counters: the list itself and the data loading stay in the caller.",
                "The scroll position is untouched after loading — right for \"load more\", wrong for replacing a page.",
                "Such a list returns badly from a link: a place in it cannot be carried in a URL. If you need URLs, use pages.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ],
              "usage": "<Pagination005 total={84} perPage={12} label=\"Load more {count}\" />"
            },
            "controls": {
              "total": {
                "label": "Total"
              },
              "perPage": {
                "label": "Batch"
              },
              "defaultShown": {
                "label": "Shown"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Load More Progress",
            "preview": {
              "props": {
                "label": "Load more {count}",
                "countText": "Showing {shown} of {total}",
                "labelText": {
                  "progress": "Shown out of the total",
                  "done": "That is everything we found"
                }
              }
            }
          }
        },
        "cardControls": [
          "defaultShown",
          "perPage",
          "total",
          "accent",
          "background"
        ]
      }
    },
    {
      "name": "pagination-006",
      "type": "registry:component",
      "title": "Маяк бесконечной ленты",
      "description": "Бесконечная лента, у которой всё-таки есть кнопка: маячок догрузки — живая кнопка, до неё доезжают табуляцией и жмут с клавиатуры.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-006/pagination-006.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-006.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-006-*. Клиентский: \"use client\" ради IntersectionObserver и состояния ленты. Наблюдатель ограничен своей областью прокрутки через root, а маячком служит сама кнопка «Загрузить ещё», поэтому догрузка работает и прокруткой, и с клавиатуры. Итог объявляется в области aria-live=\"polite\". Кнопка «Наверх» возвращает область прокрутки в начало.",
      "meta": {
        "tags": ["pagination", "infinite", "observer", "a11y"],
        "ai": {
          "export": "Pagination006",
          "usage": "<Pagination006 total={60} batch={10} height=\"13rem\" />",
          "summary": "Бесконечная лента с догрузкой по прокрутке: IntersectionObserver следит за маячком в конце списка, а маячок — это обычная кнопка, поэтому лента доступна и без мыши. Внизу строка состояния с числом загруженного и возврат наверх. Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-006-* и не подменять её токенами вашей темы (bg-card, bg-muted и подобными)",
            "маячок кнопкой, а не пустым div'ом: без мыши и без прокрутки бесконечная лента иначе просто заканчивается",
            "IntersectionObserver вместо обработчика scroll: пересчёт делает браузер, а не каждый кадр ваш код",
            "root у наблюдателя, указывающий на область прокрутки: без него слежение идёт за окном и срабатывает не там",
            "aria-live=\"polite\" у строки состояния — догрузка обязана быть объявлена",
            "отключение наблюдателя после конца списка: иначе он продолжает висеть на удалённом узле",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку"
          ],
          "adapt": [
            "total и batch — общее количество и размер порции догрузки",
            "height — высота области прокрутки",
            "содержимое строк ленты",
            "акцент через проп accent — цвет колец фокуса и ссылки «Наверх»",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "подписи через labelText: строки ленты, кнопка догрузки, строка состояния и «Наверх» — в шаблонах {count}, {shown} и {total}"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Данные не загружаются: компонент показывает механику, запрос за строками остаётся вашим.",
            "Бесконечная лента ломает подвал страницы: до него не долистать. Здесь лента ограничена своей областью, но в макете страницы это стоит помнить.",
            "Место в ленте нельзя передать ссылкой: для этого нужны страницы.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "total",
            "label": "Всего записей",
            "type": "number",
            "default": 60,
            "min": 10,
            "max": 500
          },
          {
            "prop": "batch",
            "label": "Порция",
            "type": "number",
            "default": 10,
            "min": 5,
            "max": 50
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "An infinite feed that still has a button: the loading beacon is a real button, reachable with Tab and pressable from the keyboard.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-006-* variables. Client component: \"use client\" for the sake of the IntersectionObserver and the feed state. The observer is confined to its own scroll area through root, and the \"Load more\" button itself serves as the beacon, so loading works both by scrolling and from the keyboard. The result is announced in an aria-live=\"polite\" region. The \"Back to top\" button returns the scroll area to the start.",
            "ai": {
              "summary": "An infinite feed that loads on scroll: an IntersectionObserver watches a beacon at the end of the list, and the beacon is an ordinary button, so the feed stays usable without a mouse. A status line with the loaded count and a back-to-top control sit below. Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-006-* palette — do not swap it for your theme tokens (bg-card, bg-muted and the like)",
                "the beacon as a button rather than an empty div: without a mouse and without scrolling an infinite feed otherwise just ends",
                "IntersectionObserver instead of a scroll handler: the browser decides when to recompute, not your code every frame",
                "the observer's root pointing at the scroll area: without it the watching happens against the window and fires in the wrong place",
                "aria-live=\"polite\" on the status line — a load has to be announced",
                "disconnecting the observer at the end of the list: otherwise it lingers on a removed node",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself"
              ],
              "adapt": [
                "total and batch — the overall count and the size of each load",
                "height — the height of the scroll area",
                "the contents of the feed rows",
                "the accent through the accent prop — it colours the focus rings and the back-to-top control",
                "background — the component background; empty means no background, taking the page colour",
                "the labels through labelText: the feed rows, the load button, the status line and back-to-top — with the {count}, {shown} and {total} placeholders"
              ],
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "No data is fetched: the component shows the mechanics, the request for rows stays yours.",
                "An infinite feed breaks a page footer: nobody ever reaches it. Here the feed is bounded by its own area, but it is worth remembering in a page layout.",
                "A place in the feed cannot be carried in a link: that needs pages.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ],
              "usage": "<Pagination006 total={60} batch={10} height=\"13rem\" />"
            },
            "controls": {
              "total": {
                "label": "Total rows"
              },
              "batch": {
                "label": "Batch"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Infinite Beacon",
            "preview": {
              "props": {
                "labelText": {
                  "feed": "Feed",
                  "row": "A feed entry — headline and date",
                  "more": "Load {count} more",
                  "loaded": "Loaded {shown} of {total}",
                  "done": "Everything loaded: {total}",
                  "top": "To top"
                }
              }
            }
          }
        },
        "cardControls": ["batch", "total", "accent", "background"]
      }
    },
    {
      "name": "pagination-007",
      "type": "registry:component",
      "title": "Навигация с размером страницы",
      "description": "Пагинация таблицы с выбором размера страницы: при смене размера первая видимая строка остаётся на месте, а не сбрасывается в начало.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-007/pagination-007.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-007.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-007-*. Клиентский: \"use client\" ради страницы и размера. При смене размера номер страницы пересчитывается от номера первой видимой строки, поэтому позиция в списке сохраняется. Стрелка у <select> нарисована двумя линейными градиентами, чтобы не тянуть иконку. В узком блоке контейнерный запрос переносит выбор размера на свою строку через order.",
      "meta": {
        "tags": ["pagination", "page size", "table", "select"],
        "preview": {
          "width": "full"
        },
        "ai": {
          "export": "Pagination007",
          "usage": "<Pagination007 total={214} defaultPerPage={20} sizes={[10, 20, 50, 100]} />",
          "summary": "Панель навигации по таблице: диапазон строк слева, стрелки с номером страницы посередине, выбор размера страницы справа. Смена размера сохраняет место в списке: номер страницы пересчитывается от первой видимой строки. Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-007-* и не подменять её токенами вашей темы (bg-input, border-input и подобными)",
            "пересчёт страницы от первой видимой строки: сброс на начало теряет место, к которому долго шли",
            "диапазон строк вместо одного номера страницы: он не врёт при смене размера",
            "aria-current=\"page\" у указателя страницы и aria-label у стрелок — символы «‹» и «›» скринридер прочитает как знаки",
            "табличные цифры в диапазоне и в номере страницы: иначе панель дёргается на каждом переходе",
            "<select> вместо своего выпадающего списка: на телефоне он открывается системным колесом",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку"
          ],
          "adapt": [
            "total и defaultPerPage — общее количество строк и начальный размер страницы",
            "массив sizes — доступные размеры",
            "акцент через проп accent — цвет колец фокуса",
            "порог контейнерного запроса, по которому панель перестраивается",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "rangeText — строка диапазона с подстановками {from}, {to} и {total}",
            "подписи через labelText — заголовок навигации, стрелки, «Показывать по» и указатель {page} / {pages}"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Компонент показывает только границы: сам запрос строк остаётся за вызывающим кодом.",
            "Размер страницы не сохраняется между визитами: хранение — за вами.",
            "Размеры больше сотни строк почти всегда бессмысленны: страница будет грузиться дольше, чем читаться.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "total",
            "label": "Всего строк",
            "type": "number",
            "default": 214,
            "min": 10,
            "max": 999
          },
          {
            "prop": "defaultPerPage",
            "label": "На странице",
            "type": "number",
            "default": 20,
            "min": 5,
            "max": 100
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "Table pagination with a page-size picker: changing the size keeps the first visible row in place instead of throwing you back to the start.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-007-* variables. Client component: \"use client\" for the sake of the page and the size. When the size changes, the page number is recomputed from the number of the first visible row, so the place in the list is preserved. The arrow on the <select> is drawn with two linear gradients so as not to pull in an icon. In a narrow block a container query moves the size picker onto a line of its own through order.",
            "ai": {
              "summary": "A table navigation bar: the row range on the left, arrows with the page number in the middle, the page-size picker on the right. Changing the size preserves your place: the page number is recomputed from the first visible row. Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-007-* palette — do not swap it for your theme tokens (bg-input, border-input and the like)",
                "recomputing the page from the first visible row: resetting to the start loses a place that took a while to reach",
                "the row range instead of a lone page number: it does not lie when the size changes",
                "aria-current=\"page\" on the page indicator and aria-labels on the arrows — a screen reader reads \"‹\" and \"›\" as symbols",
                "tabular figures in the range and the page number: otherwise the bar jitters on every step",
                "a <select> instead of a custom drop-down: on a phone it opens as the system wheel",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself"
              ],
              "adapt": [
                "total and defaultPerPage — the row count and the initial page size",
                "the sizes array — the available page sizes",
                "the accent through the accent prop — it colours the focus rings",
                "the container-query threshold where the bar rearranges",
                "background — the component background; empty means no background, taking the page colour",
                "rangeText — the range string with the {from}, {to} and {total} placeholders",
                "the labels through labelText — the nav title, the arrows, the size caption and the {page} / {pages} indicator"
              ],
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The component only shows the bounds: fetching the rows stays with the caller.",
                "The page size is not remembered between visits: persisting it is up to you.",
                "Sizes beyond a hundred rows are almost always pointless: the page takes longer to load than to read.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ],
              "usage": "<Pagination007 total={214} defaultPerPage={20} sizes={[10, 20, 50, 100]} />"
            },
            "controls": {
              "total": {
                "label": "Total rows"
              },
              "defaultPerPage": {
                "label": "Per page"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Page Size Pagination",
            "preview": {
              "props": {
                "rangeText": "{from}–{to} of {total}",
                "labelText": {
                  "nav": "List navigation",
                  "prev": "Previous page",
                  "next": "Next page",
                  "size": "Rows per page",
                  "where": "{page} / {pages}"
                }
              }
            }
          }
        },
        "cardControls": ["defaultPerPage", "total", "background"]
      }
    },
    {
      "name": "pagination-008",
      "type": "registry:component",
      "title": "Переход к странице",
      "description": "Пагинация с полем перехода: номер вводится и отправляется формой, а неверное значение не молчит, а объясняет допустимый диапазон.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-008/pagination-008.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-008.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-008-*. Клиентский: \"use client\" ради страницы и черновика ввода. Поле перехода лежит в настоящей <form>, поэтому Enter отправляет её без отдельного обработчика клавиши. Неверный номер помечает поле через aria-invalid и показывает сообщение с role=\"alert\", связанное с полем через aria-describedby; то же место в спокойном состоянии занимает подсказка об общем числе страниц.",
      "meta": {
        "tags": ["pagination", "jump", "form", "validation"],
        "preview": {
          "width": "full"
        },
        "ai": {
          "export": "Pagination008",
          "usage": "<Pagination008 total={148} defaultPage={37} label=\"Перейти к странице\" />",
          "summary": "Пагинация для длинного списка: стрелки с указателем страницы сверху и поле перехода к произвольному номеру снизу. Ввод проверяется, ошибка объясняется словами и связана с полем. Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-008-* и не подменять её токенами вашей темы (bg-input, border-destructive и подобными)",
            "настоящую <form> вокруг поля: Enter отправляет её сам, отдельный обработчик клавиши не нужен и не забудется",
            "aria-describedby, связывающий поле с подсказкой и с сообщением об ошибке — иначе текст рядом с полем для скринридера просто абзац",
            "role=\"alert\" у сообщения об ошибке: без него оно не объявляется в момент появления",
            "подсказку об общем числе страниц в спокойном состоянии — место под сообщение занято всегда, и раскладка не прыгает",
            "<label> у поля вместо одного placeholder: подсказка исчезает при вводе и именем поля быть не может",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку"
          ],
          "adapt": [
            "total и defaultPage — количество страниц и начальную",
            "label — подпись поля перехода",
            "тексты подсказки и сообщения об ошибке",
            "акцент через проп accent — цвет кнопки «Перейти» и колец фокуса",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "nowText — указатель страницы с подстановками {page} и {total}",
            "подписи через labelText — стрелки, кнопка «Перейти», подсказка и текст ошибки"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Компонент клиентский: состояние страницы и черновик ввода живут внутри.",
            "Поле не ограничивает ввод жёстко: атрибуты min и max — подсказка браузеру, проверка всё равно нужна в обработчике.",
            "Поле перехода окупается на списках от нескольких десятков страниц: на пяти оно только мешает.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "total",
            "label": "Всего страниц",
            "type": "number",
            "default": 148,
            "min": 1,
            "max": 999
          },
          {
            "prop": "defaultPage",
            "label": "Страница",
            "type": "number",
            "default": 37,
            "min": 1,
            "max": 148
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "Pagination with a jump field: the number is typed and submitted by a form, and a wrong value explains the allowed range instead of staying silent.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-008-* variables. Client component: \"use client\" for the sake of the page and the input draft. The jump field sits in a real <form>, so Enter submits it without a separate key handler. A wrong number marks the field through aria-invalid and shows a message with role=\"alert\" linked to the field through aria-describedby; in the calm state the same slot is taken by the hint about the total number of pages.",
            "ai": {
              "summary": "Pagination for a long list: arrows with a page indicator on top and a jump-to-page field below. Input is validated, the error is spelled out in words and tied to the field. Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-008-* palette — do not swap it for your theme tokens (bg-input, border-destructive and the like)",
                "the real <form> around the field: Enter submits it by itself, so a separate key handler is neither needed nor forgotten",
                "the aria-describedby linking the field to the hint and to the error — otherwise the text beside the field is just a paragraph",
                "role=\"alert\" on the error message: without it the message is not announced when it appears",
                "the hint about the page count in the calm state — the message slot is always occupied, so the layout never jumps",
                "a <label> on the field rather than a placeholder alone: a placeholder disappears while typing and cannot be the name",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself"
              ],
              "adapt": [
                "total and defaultPage — the page count and the starting page",
                "label — the caption of the jump field",
                "the hint and error texts",
                "the accent through the accent prop — it colours the Go button and the focus rings",
                "background — the component background; empty means no background, taking the page colour",
                "nowText — the page indicator with the {page} and {total} placeholders",
                "the labels through labelText — the arrows, the Go button, the hint and the error text"
              ],
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The component is a client one: the page state and the input draft live inside it.",
                "The field does not hard-limit typing: min and max are hints to the browser, the check still belongs in the handler.",
                "A jump field pays off on lists of dozens of pages: on five it only gets in the way.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ],
              "usage": "<Pagination008 total={148} defaultPage={37} label=\"Go to page\" />"
            },
            "controls": {
              "total": {
                "label": "Total pages"
              },
              "defaultPage": {
                "label": "Page"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Jump To Page",
            "preview": {
              "props": {
                "label": "Go to page",
                "nowText": "Page {page} of {total}",
                "labelText": {
                  "nav": "Page navigation",
                  "prev": "← Back",
                  "next": "Forward →",
                  "go": "Go",
                  "error": "Enter a number from 1 to {total}",
                  "hint": "Pages in total: {total}"
                }
              }
            }
          }
        },
        "cardControls": ["defaultPage", "total", "accent", "background"]
      }
    },
    {
      "name": "pagination-009",
      "type": "registry:component",
      "title": "Навигация миниатюрами",
      "description": "Пагинация для телефона: три крупные цели вместо ряда номеров, а полный список страниц открывается сеткой в popover.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-009/pagination-009.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-009.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-009-*. Серверный: хуков нет, все номера — обычные ссылки. Цели по 2.75rem — нижняя граница для касания пальцем. Сетка страниц лежит в popover=\"auto\" с якорем на самой панели, поэтому закрытие по Escape и по касанию мимо достаётся от браузера. Текущая страница помечена aria-current=\"page\", края гасятся через aria-disabled.",
      "meta": {
        "tags": ["pagination", "mobile", "touch", "popover"],
        "ai": {
          "export": "Pagination009",
          "usage": "<Pagination009 page={3} total={18} hrefOf={(page) => `/catalog?page=${page}`} />",
          "summary": "Компактная пагинация для телефона: крупные стрелки по краям и кнопка с текущей страницей посередине, открывающая сетку всех номеров. Все переходы — ссылки, поэтому страница открывается в новой вкладке и остаётся в истории. Ноль зависимостей, один файл, клиентского JS нет.",
          "preserve": [
            "локальную палитру --vibeui-pagination-009-* и не подменять её токенами вашей темы (bg-card, border-input и подобными)",
            "размер целей не меньше 2.75rem: ряд из мелких номеров на телефоне не берётся пальцем",
            "сетку номеров в popover вместо ряда: восемнадцать номеров в строку на узком экране не помещаются",
            "popover=\"auto\": Escape и касание мимо закрывают сетку без вашего кода",
            "aria-current=\"page\" у текущего номера и в списке, и на кнопке-указателе",
            "aria-label у стрелок: стрелка из бордюров не имеет текста и без подписи безымянна",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку",
            "непрозрачную подложку у сетки в popover: всплывающая панель обязана перекрывать содержимое под собой"
          ],
          "adapt": [
            "page и total — текущая страница и общее количество",
            "hrefOf — как собрать адрес страницы под ваш роутер",
            "акцент через проп accent — цвет активного номера и колец фокуса",
            "размер целей и число колонок в сетке",
            "background — подложка панели и сетки; пусто означает «без подложки», по цвету страницы",
            "подписи через labelText — заголовок навигации, стрелки, сетка и строка «из {total}»"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Сетка выводит все страницы: на нескольких сотнях она станет длинным списком с прокруткой.",
            "Без поддержки anchor positioning сетка открывается по центру экрана: на телефоне это приемлемо, но проверьте.",
            "Компонент рассчитан на узкую ширину: на десктопе берите ряд номеров.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "page",
            "label": "Страница",
            "type": "number",
            "default": 3,
            "min": 1,
            "max": 18
          },
          {
            "prop": "total",
            "label": "Всего",
            "type": "number",
            "default": 18,
            "min": 1,
            "max": 99
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "Pagination for a phone: three large targets instead of a row of numbers, with the full page list opening as a grid in a popover.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-009-* variables. Server rendered: no hooks, every number is an ordinary link. The targets are 2.75rem — the lower bound for a finger tap. The grid of pages sits in a popover=\"auto\" anchored to the bar itself, so closing on Escape and on a tap outside comes from the browser. The current page is marked with aria-current=\"page\", and the edges dim through aria-disabled.",
            "ai": {
              "summary": "Compact phone pagination: large arrows at the edges and a current-page button in the middle that opens a grid of every number. Every step is a link, so a page opens in a new tab and stays in history. Zero dependencies, one file, no client JS.",
              "preserve": [
                "the local --vibeui-pagination-009-* palette — do not swap it for your theme tokens (bg-card, border-input and the like)",
                "targets no smaller than 2.75rem: a row of small numbers cannot be hit with a thumb",
                "the number grid in a popover instead of a row: eighteen numbers do not fit across a narrow screen",
                "popover=\"auto\": Escape and a tap outside close the grid without your code",
                "aria-current=\"page\" on the current number both in the list and on the indicator button",
                "the aria-labels on the arrows: a border-drawn arrow has no text and is nameless without one",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself",
                "the opaque background of the popover grid: a floating panel has to cover what is underneath it"
              ],
              "adapt": [
                "page and total — the current page and the count",
                "hrefOf — how to build a page address for your router",
                "the accent through the accent prop — it colours the active number and the focus rings",
                "the target size and the number of grid columns",
                "background — the bar and grid background; empty means no background, taking the page colour",
                "the labels through labelText — the nav title, the arrows, the grid and the \"of {total}\" line"
              ],
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The grid lists every page: at several hundred it becomes a long scrolling list.",
                "Without anchor positioning support the grid opens centred: acceptable on a phone, but check it.",
                "The component is meant for narrow widths: on desktop take the row of numbers.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ],
              "usage": "<Pagination009 page={3} total={18} hrefOf={(page) => `/catalog?page=${page}`} />"
            },
            "controls": {
              "page": {
                "label": "Page"
              },
              "total": {
                "label": "Total"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Thumb Pagination",
            "preview": {
              "props": {
                "labelText": {
                  "nav": "Page navigation",
                  "prev": "Previous page",
                  "next": "Next page",
                  "sheet": "All pages",
                  "of": "of {total}"
                }
              }
            }
          }
        },
        "cardControls": ["page", "total", "background"]
      }
    },
    {
      "name": "pagination-010",
      "type": "registry:component",
      "title": "Ползунок с многоточием",
      "description": "Пагинация с многоточием для сотен страниц: клик по «…» открывает ползунок по скрытому диапазону, а число под ним меняется вживую, пока курсор или палец двигают ручку.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-010/pagination-010.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-010.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-010-*. Клиентский: \"use client\" ради страницы и открытого разрыва. Окно номеров вокруг текущей страницы считает windowOf, а каждый разрыв несёт границы скрытого диапазона. Клик по «…» заменяет её на <input type=\"range\"> с этими границами и кнопкой «Перейти»: значение меняется вживую в <output>, а переход подтверждается явно, чтобы работать одинаково с мышью и с клавиатуры (Enter подтверждает, Escape закрывает).",
      "meta": {
        "tags": ["pagination", "ellipsis", "scrubber", "range"],
        "ai": {
          "export": "Pagination010",
          "usage": "<Pagination010 page={53} total={240} siblings={1} />",
          "summary": "Пагинация со свёрткой середины для сотен страниц: вместо статичного «…» или прыжка фиксированным шагом ползунок открывает точный доступ к любому номеру в скрытом диапазоне. Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-010-* и не подменять её токенами вашей темы (bg-primary, border-input и подобными)",
            "реальные <button> для номеров, краёв и кнопки «…» — ползунок открывается и с клавиатуры, а не только по клику мыши",
            "кнопку «Перейти» рядом с range: без неё мышью подтвердить выбор, не трогая клавиатуру, нечем",
            "aria-label у ползунка с границами скрытого диапазона — иначе значение остаётся без объяснения для скринридера",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку"
          ],
          "adapt": [
            "page, total и siblings — начальную страницу, их общее число и ширину окна вокруг текущей",
            "акцент через проп accent — цвет заливки кнопок, ползунка и колец фокуса",
            "тексты aria-label кнопок и подсказки диапазона",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "подписи через labelText — стрелки, номера, ползунок и кнопки «Перейти» и «Закрыть»"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Состояние страницы живёт внутри компонента: чтобы поднять его наверх, замените useState на пропсы со своим обработчиком.",
            "Ползунок окупается на сотнях страниц; на десятке он сложнее обычного ряда номеров.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "page",
            "label": "Страница",
            "type": "number",
            "default": 53,
            "min": 1,
            "max": 240
          },
          {
            "prop": "total",
            "label": "Всего",
            "type": "number",
            "default": 240,
            "min": 1,
            "max": 999
          },
          {
            "prop": "siblings",
            "label": "Соседей",
            "type": "number",
            "default": 1,
            "min": 0,
            "max": 3
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "Ellipsis pagination for hundreds of pages: clicking \"…\" opens a scrubber over the hidden range, and the number below it updates live while a cursor or finger drags the handle.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-010-* variables. Client component: \"use client\" for the sake of the page and the opened gap. The window of numbers around the current page is computed by windowOf, and every gap carries the bounds of the hidden range. Clicking the \"…\" replaces it with an <input type=\"range\"> holding those bounds and a \"Go\" button: the value changes live in an <output>, and the jump is confirmed explicitly so that it works the same with a mouse and from the keyboard (Enter confirms, Escape closes).",
            "ai": {
              "summary": "Middle-collapsing pagination for hundreds of pages: instead of a static \"…\" or a fixed-step jump, a scrubber gives exact access to any number in the hidden range. Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-010-* palette — do not swap it for your theme tokens (bg-primary, border-input and the like)",
                "real <button> elements for numbers, edges and the ellipsis trigger — the scrubber opens from the keyboard too, not only by a mouse click",
                "the Go button beside the range — without it a mouse user has no way to confirm a pick without touching the keyboard",
                "the aria-label on the range naming the hidden range's bounds — otherwise the value stays unexplained for a screen reader",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself"
              ],
              "adapt": [
                "page, total and siblings — the starting page, their total count and the window width around the current one",
                "the accent through the accent prop — it colours the buttons' fill, the range and the focus rings",
                "the aria-label wording on the buttons and the range hint",
                "background — the component background; empty means no background, taking the page colour",
                "the labels through labelText — the arrows, the numbers, the scrubber and the Go and Close buttons"
              ],
              "usage": "<Pagination010 page={53} total={240} siblings={1} />",
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The page state lives inside the component: to lift it up, replace useState with props and your own handler.",
                "The scrubber pays off across hundreds of pages; on a dozen it is more complicated than a plain row of numbers.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ]
            },
            "controls": {
              "page": {
                "label": "Page"
              },
              "total": {
                "label": "Total"
              },
              "siblings": {
                "label": "Siblings"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Ellipsis Scrubber",
            "preview": {
              "props": {
                "labelText": {
                  "nav": "Result pages",
                  "prev": "Previous page",
                  "next": "Next page",
                  "page": "Page {page}",
                  "range": "Page from {from} to {to}",
                  "showRange": "Show pages {from} through {to}",
                  "go": "Go",
                  "close": "Close the scrubber"
                }
              }
            }
          }
        },
        "cardControls": ["page", "siblings", "total", "accent", "background"]
      }
    },
    {
      "name": "pagination-011",
      "type": "registry:component",
      "title": "«Показать ещё» с кольцом",
      "description": "«Показать ещё» без полосы прогресса: доля показанного заполняет кольцо самой кнопки, число внутри называет следующую порцию, а счётчик снизу сообщает «60 из 240».",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-011/pagination-011.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-011.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-011-*. Клиентский: \"use client\" ради счётчика показанного. Кольцо — это conic-gradient на самой кнопке, доля вычисляется в проценте и передаётся через CSS-переменную --vibeui-pagination-011-percent, как ширина полосы в других вариантах «показать ещё». Внутренний круг того же цвета, что фон карточки, вырезает кольцо визуально. Дочитанный список отключает кнопку и меняет содержимое на галочку, а счётчик снизу остаётся источником текста для aria-live.",
      "meta": {
        "tags": ["pagination", "load more", "progress", "ring"],
        "ai": {
          "export": "Pagination011",
          "usage": "<Pagination011 total={240} batch={60} defaultShown={60} />",
          "summary": "«Показать ещё» в виде кольцевого индикатора: доля показанного заполняет саму кнопку коническим градиентом, число внутри называет следующую порцию, а подпись снизу читает «60 из 240». Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-011-* и не подменять её токенами вашей темы (bg-accent, text-muted-foreground и подобными)",
            "conic-gradient через CSS-переменную --vibeui-pagination-011-percent — процент считается в JS и не выражается голым классом",
            "aria-live у подписи «Показано N из total» — без него смена счётчика проходит мимо скринридера",
            "disabled и смену содержимого кольца на галочку в конце списка — кнопка не должна оставаться кликабельной без дела",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку",
            "непрозрачный внутренний круг кольца: именно он вырезает кольцо, и прозрачным он превратит его в диск"
          ],
          "adapt": [
            "total, batch и defaultShown — общее число, размер порции и то, сколько показано изначально",
            "акцент через проп accent — цвет заливки кольца",
            "текст внутри кольца и подпись aria-label кнопки",
            "background — подложка компонента и внутреннего круга кольца; пусто означает «без подложки», по цвету страницы",
            "countText — строка счётчика с подстановками {shown} и {total}",
            "подписи кнопки через labelText — {count} и {total} подставляются"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Компонент показывает только счётчики: сам список и загрузку данных держит вызывающий код.",
            "Внутренний круг кольца непрозрачен: на цветной странице задайте background, чтобы он совпал с подложкой.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "total",
            "label": "Всего",
            "type": "number",
            "default": 240,
            "min": 1,
            "max": 999
          },
          {
            "prop": "batch",
            "label": "Порция",
            "type": "number",
            "default": 60,
            "min": 1,
            "max": 200
          },
          {
            "prop": "defaultShown",
            "label": "Показано сразу",
            "type": "number",
            "default": 60,
            "min": 0,
            "max": 240
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "A \"load more\" pattern without a progress bar: the shown share fills the button's own ring, the number inside names the next chunk, and the counter below reads \"60 of 240\".",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-011-* variables. Client component: \"use client\" for the sake of the counter of what is shown. The ring is a conic-gradient on the button itself, and the share is computed as a percentage and passed through the --vibeui-pagination-011-percent CSS variable, like the bar width in the other \"load more\" variants. The inner circle, in the same colour as the card background, cuts the ring out visually. A list read to the end disables the button and changes its contents to a checkmark, while the counter below remains the text source for aria-live.",
            "ai": {
              "summary": "A ring-shaped \"load more\": the shown share fills the button itself with a conic gradient, the number inside names the next chunk, and the caption below reads \"60 of 240\". Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-011-* palette — do not swap it for your theme tokens (bg-accent, text-muted-foreground and the like)",
                "the conic-gradient driven by the --vibeui-pagination-011-percent custom property — the percentage is computed in JS and cannot be expressed as a plain class",
                "the aria-live on the \"Shown N of total\" caption — without it the counter change passes the screen reader by",
                "the disabled state and the ring's switch to a checkmark at the end of the list — the button must not stay clickable with nothing left to do",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself",
                "the opaque inner circle of the ring: it is what cuts the ring out, and making it transparent turns the ring into a disc"
              ],
              "adapt": [
                "total, batch and defaultShown — the total count, the chunk size and how much is shown initially",
                "the accent through the accent prop — it colours the ring's fill",
                "the text inside the ring and the button's aria-label",
                "background — the background of the component and of the ring's inner circle; empty means no background, taking the page colour",
                "countText — the counter string with the {shown} and {total} placeholders",
                "the button labels through labelText — {count} and {total} are substituted"
              ],
              "usage": "<Pagination011 total={240} batch={60} defaultShown={60} />",
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The component only shows the counters: the list itself and the data loading stay in the caller.",
                "The ring's inner circle is opaque: on a coloured page set background so that it matches the surface.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ]
            },
            "controls": {
              "total": {
                "label": "Total"
              },
              "batch": {
                "label": "Batch"
              },
              "defaultShown": {
                "label": "Shown at start"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Load More Ring",
            "preview": {
              "props": {
                "countText": "Showing {shown} of {total}",
                "labelText": {
                  "done": "Everything loaded: {total}",
                  "more": "Load {count} more out of {total}"
                }
              }
            }
          }
        },
        "cardControls": [
          "defaultShown",
          "batch",
          "total",
          "accent",
          "background"
        ]
      }
    },
    {
      "name": "pagination-012",
      "type": "registry:component",
      "title": "Автоподгрузка при прокрутке",
      "description": "Бесконечная лента без кнопки на виду: подгрузка запускается прокруткой до низа, во время запроса внизу пульсируют точки, а спрятанная фокусируемая кнопка остаётся запасным способом для клавиатуры.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-012/pagination-012.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-012.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-012-*. Клиентский: \"use client\" ради состояния подгрузки. onScroll на области ленты считает расстояние до низа и запускает setTimeout, имитирующий сетевой запрос, — на время ожидания внизу пульсируют три точки, а текст рядом уходит в aria-live. Кнопки на виду нет, но она не исчезла: визуально скрытая (clip), она становится видимой и рабочей по фокусу — клавиатура и скринридер получают тот же результат, что и прокрутка.",
      "meta": {
        "tags": ["pagination", "infinite", "scroll", "loader"],
        "ai": {
          "export": "Pagination012",
          "usage": "<Pagination012 total={90} batch={15} height=\"13rem\" />",
          "summary": "Бесконечная лента с автоматической подгрузкой по прокрутке: кнопки на виду нет, вместо неё внизу пульсируют точки на время запроса. Спрятанная, но фокусируемая кнопка остаётся рабочим способом догрузки с клавиатуры. Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-012-* и не подменять её токенами вашей темы (bg-muted, text-foreground и подобными)",
            "спрятанную, но фокусируемую кнопку-запасной вариант — без неё подгрузка недоступна с клавиатуры и для скринридера",
            "aria-live у статуса загрузки — иначе смена «Загрузка… → Показано N из total» проходит мимо скринридера",
            "порог в onScroll до срабатывания подгрузки — без него запрос уходит либо слишком поздно, либо на каждый пиксель прокрутки",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку"
          ],
          "adapt": [
            "total, batch и height — общее число записей, размер порции и высоту прокручиваемой ленты",
            "delay — задержку имитации запроса",
            "акцент через проп accent — цвет точек загрузки",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "подписи через labelText — строки ленты, статус загрузки и запасная кнопка; {shown} и {total} подставляются"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Данные не загружаются: setTimeout здесь имитирует запрос, настоящий остаётся вашим.",
            "Место в ленте нельзя передать ссылкой: для этого нужны страницы.",
            "Не выносите CSS-переменные в globals.css: компонент обязан оставаться одним файлом."
          ]
        },
        "controls": [
          {
            "prop": "total",
            "label": "Всего",
            "type": "number",
            "default": 90,
            "min": 1,
            "max": 999
          },
          {
            "prop": "batch",
            "label": "Порция",
            "type": "number",
            "default": 15,
            "min": 1,
            "max": 60
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Подложка",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "An infinite feed with no visible button: loading starts when the scroll reaches the bottom, pulsing dots show at the bottom while the request is in flight, and a hidden but focusable button stays as a keyboard fallback.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-012-* variables. Client component: \"use client\" for the sake of the loading state. An onScroll on the feed area measures the distance to the bottom and starts a setTimeout that simulates a network request — while the wait lasts, three dots pulse at the bottom and the text beside them goes into aria-live. There is no button in sight, but it has not gone away: visually hidden (clip), it becomes visible and working on focus — the keyboard and the screen reader get the same result as scrolling.",
            "ai": {
              "summary": "An infinite feed that loads automatically on scroll: there is no visible button, pulsing dots appear at the bottom while a request is pending instead. A visually hidden but focusable button stays a working keyboard fallback. Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-012-* palette — do not swap it for your theme tokens (bg-muted, text-foreground and the like)",
                "the visually hidden but focusable fallback button — without it loading is unreachable from the keyboard and for a screen reader",
                "the aria-live on the loading status — otherwise the \"Loading… → Shown N of total\" change passes the screen reader by",
                "the threshold in onScroll before loading fires — without it the request fires either too late or on every pixel of scroll",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself"
              ],
              "adapt": [
                "total, batch and height — the total record count, the chunk size and the scrollable feed's height",
                "delay — the simulated request's delay",
                "the accent through the accent prop — it colours the loading dots",
                "background — the component background; empty means no background, taking the page colour",
                "the labels through labelText — the feed rows, the loading status and the fallback button; {shown} and {total} are substituted"
              ],
              "usage": "<Pagination012 total={90} batch={15} height=\"13rem\" />",
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "No data is fetched: the setTimeout here simulates a request, the real one stays yours.",
                "A place in the feed cannot be carried in a link: that needs pages.",
                "Do not lift the CSS variables into globals.css: the component has to stay a single file."
              ]
            },
            "controls": {
              "total": {
                "label": "Total"
              },
              "batch": {
                "label": "Batch"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Infinite Auto Scroll",
            "preview": {
              "props": {
                "labelText": {
                  "feed": "Record feed",
                  "row": "A feed entry — headline and date",
                  "loading": "Loading…",
                  "shown": "Showing {shown} of {total}",
                  "done": "All records shown: {total}",
                  "more": "Load more"
                }
              }
            }
          }
        },
        "cardControls": ["batch", "total", "background"]
      }
    },
    {
      "name": "pagination-013",
      "type": "registry:component",
      "title": "Переключатель размера страницы",
      "description": "Пагинация таблицы, где размер страницы — группа кнопок рядом с номерами, а не спрятанный выпадающий список: текущий выбор виден без клика, а место в списке сохраняется при смене размера.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-013/pagination-013.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-013.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-013-*. Клиентский: \"use client\" ради страницы и размера страницы. Номера считает windowOf — тот же приём, что и в других вариантах с окном вокруг текущей страницы. Размер переключает role=\"group\" из кнопок с aria-pressed, а не <select>: выбор виден сразу, без раскрытия списка. changeSize пересчитывает номер страницы от текущей первой видимой строки, чтобы смена размера не отбрасывала на первую страницу.",
      "meta": {
        "tags": ["pagination", "page size", "table", "toggle"],
        "ai": {
          "export": "Pagination013",
          "usage": "<Pagination013 total={326} defaultPerPage={25} sizes={[10, 25, 50, 100]} />",
          "summary": "Номера страниц и группа кнопок размера страницы в одном ряду: размер переключается кнопками с видимым нажатым состоянием, а не выпадающим списком, и первая видимая строка остаётся на месте при смене размера. Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-013-* и не подменять её токенами вашей темы (bg-secondary, border-border и подобными)",
            "role=\"group\" с aria-label и aria-pressed у кнопок размера — без них группа читается как набор несвязанных кнопок",
            "пересчёт страницы от первой видимой строки в changeSize — иначе смена размера отбрасывает читателя в начало списка",
            "aria-current=\"page\" у активного номера и реальные <button> для всех целей ряда",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку",
            "контрастный текст на акценте через --vibeui-pagination-013-on-accent: в тёмной теме акцент светлеет, и белая цифра на нём перестаёт читаться"
          ],
          "adapt": [
            "total, defaultPerPage и sizes — общее число строк, размер по умолчанию и доступные размеры",
            "siblings — ширину окна номеров вокруг текущей страницы",
            "акцент через проп accent — цвет активного номера и активной кнопки размера",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "подписи navLabel, prevLabel, nextLabel, pageLabel и sizeGroupLabel — по умолчанию русские, язык задаёт проект"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Кнопки размера — не <select>: если размеров больше пяти, выпадающий список честнее ряда, который переносится на вторую строку."
          ]
        },
        "controls": [
          {
            "prop": "total",
            "label": "Всего строк",
            "type": "number",
            "default": 326,
            "min": 1,
            "max": 9999
          },
          {
            "prop": "defaultPerPage",
            "label": "Размер страницы",
            "type": "number",
            "default": 25,
            "min": 1,
            "max": 100
          },
          {
            "prop": "siblings",
            "label": "Соседей",
            "type": "number",
            "default": 1,
            "min": 0,
            "max": 3
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Фон",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "Table pagination where the page size is a button group beside the page numbers, not a hidden dropdown: the current choice is visible without a click, and the spot in the list survives a size change.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-013-* variables. Client component: \"use client\" for the sake of the page and the page size. The numbers are computed by windowOf — the same technique as in the other variants with a window around the current page. The size is switched by a role=\"group\" of buttons with aria-pressed rather than by a <select>: the choice is visible at once, without opening a list. changeSize recomputes the page number from the current first visible row, so that a size change does not throw the reader back to the first page.",
            "ai": {
              "summary": "Page numbers and a page-size button group in one row: the size switches via buttons with a visible pressed state instead of a dropdown, and the first visible row stays put when the size changes. Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-013-* palette — do not swap it for your theme tokens (bg-secondary, border-border and the like)",
                "the role=\"group\" with aria-label and aria-pressed on the size buttons — without them the group reads as a set of unrelated buttons",
                "the recalculation from the first visible row in changeSize — otherwise a size change drops the reader back to the start of the list",
                "aria-current=\"page\" on the active number and real <button> elements for every target in the row",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself",
                "the contrast text on the accent through --vibeui-pagination-013-on-accent: in the dark theme the accent lightens and a white digit on it stops reading"
              ],
              "adapt": [
                "total, defaultPerPage and sizes — the total row count, the default size and the available sizes",
                "siblings — the width of the number window around the current page",
                "the accent through the accent prop — it colours the active number and the active size button",
                "background — the component background; empty means no background, taking the page colour",
                "the navLabel, prevLabel, nextLabel, pageLabel and sizeGroupLabel captions — Russian by default, the project sets the language"
              ],
              "usage": "<Pagination013 total={326} defaultPerPage={25} sizes={[10, 25, 50, 100]} />",
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The size buttons are not a <select>: with more than five sizes a dropdown is more honest than a row that wraps onto a second line."
              ]
            },
            "controls": {
              "total": {
                "label": "Total rows"
              },
              "defaultPerPage": {
                "label": "Page size"
              },
              "siblings": {
                "label": "Siblings"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Size Toggle Pagination",
            "preview": {
              "props": {
                "navLabel": "List navigation",
                "prevLabel": "Previous page",
                "nextLabel": "Next page",
                "pageLabel": "Page {page}",
                "sizeGroupLabel": "Rows per page"
              }
            }
          }
        },
        "cardControls": [
          "defaultPerPage",
          "siblings",
          "total",
          "accent",
          "background"
        ]
      }
    },
    {
      "name": "pagination-014",
      "type": "registry:component",
      "title": "Сворачиваемая навигация",
      "description": "Одна пагинация на широкий и узкий контейнер: ряд номеров сам уступает место паре стрелок и счётчику «страница из страниц», когда компоненту достаётся мало места — по ширине блока, а не окна.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-014/pagination-014.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-014.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-014-*. Клиентский: \"use client\" ради текущей страницы. Корень объявляет container-type: inline-size вне @container — правило внутри контейнерного запроса не может целиться в сам блок, поэтому свойство ставится на верхнеуровневый селектор. Ниже порога @container ряд номеров (data-part=\"numbers\") прячется через display:none, а счётчик (data-part=\"compact\") занимает его место; оба элемента используют одни и те же кнопки «назад»/«вперёд».",
      "meta": {
        "tags": ["pagination", "responsive", "container", "compact"],
        "ai": {
          "export": "Pagination014",
          "usage": "<Pagination014 page={4} total={48} siblings={1} />",
          "summary": "Пагинация, которая сама сворачивается до стрелок и счётчика в узком контейнере — по ширине блока через @container, а не по ширине окна браузера. В карточке или боковой панели превращается в компактный вид без отдельного компонента для мобильных. Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-014-* и не подменять её токенами вашей темы (bg-card, text-muted-foreground и подобными)",
            "container-type: inline-size на корневом селекторе вне @container — внутри контейнерного запроса нельзя стилизовать сам блок",
            "display:none у скрытого варианта, а не visibility или opacity — иначе скринридер озвучивает оба представления сразу",
            "общие кнопки «назад»/«вперёд» для обоих состояний — раскладка переключается только серединой ряда",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку",
            "контрастный текст на акценте через --vibeui-pagination-014-on-accent: в тёмной теме акцент светлеет, и белая цифра на нём перестаёт читаться"
          ],
          "adapt": [
            "page, total и siblings — начальную страницу, их общее число и ширину окна вокруг текущей",
            "порог в @container (max-width: 21rem) — под какой шириной блока включается компактный вид",
            "акцент через проп accent — цвет активного номера и колец фокуса",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы",
            "подписи navLabel, prevLabel, nextLabel, pageLabel и compactLabel — по умолчанию русские, язык задаёт проект"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "Порог компактного вида живёт в @container внутри блока <style>: пропом он быть не может — контейнерный запрос не читает CSS-переменные в условии."
          ]
        },
        "controls": [
          {
            "prop": "page",
            "label": "Страница",
            "type": "number",
            "default": 4,
            "min": 1,
            "max": 48
          },
          {
            "prop": "total",
            "label": "Всего",
            "type": "number",
            "default": 48,
            "min": 1,
            "max": 999
          },
          {
            "prop": "siblings",
            "label": "Соседей",
            "type": "number",
            "default": 1,
            "min": 0,
            "max": 3
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Фон",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "One pagination for a wide and a narrow container: the row of numbers steps aside for a pair of arrows and a \"page of pages\" counter once the component gets little room — by the block's width, not the window's.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-014-* variables. Client component: \"use client\" for the sake of the current page. The root declares container-type: inline-size outside @container — a rule inside a container query cannot target the block itself, so the property is set on the top-level selector. Below the @container threshold the row of numbers (data-part=\"numbers\") is hidden through display:none and the counter (data-part=\"compact\") takes its place; both elements use the same back and forward buttons.",
            "ai": {
              "summary": "Pagination that collapses itself down to arrows and a counter in a narrow container — by the block's own width via @container, not the browser window's. Inside a card or a sidebar it becomes the compact view without a separate mobile component. Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-014-* palette — do not swap it for your theme tokens (bg-card, text-muted-foreground and the like)",
                "container-type: inline-size on the root selector outside @container — a rule inside a container query cannot style the block itself",
                "display:none on the hidden variant rather than visibility or opacity — otherwise a screen reader announces both representations at once",
                "the shared prev/next buttons for both states — only the middle of the row switches layout",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself",
                "the contrast text on the accent through --vibeui-pagination-014-on-accent: in the dark theme the accent lightens and a white digit on it stops reading"
              ],
              "adapt": [
                "page, total and siblings — the starting page, their total count and the window width around the current one",
                "the threshold in @container (max-width: 21rem) — below which block width the compact view kicks in",
                "the accent through the accent prop — it colours the active number and the focus rings",
                "background — the component background; empty means no background, taking the page colour",
                "the navLabel, prevLabel, nextLabel, pageLabel and compactLabel captions — Russian by default, the project sets the language"
              ],
              "usage": "<Pagination014 page={4} total={48} siblings={1} />",
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The compact threshold lives in the @container inside the <style> block: it cannot become a prop, because a container query does not read CSS variables in its condition."
              ]
            },
            "controls": {
              "page": {
                "label": "Page"
              },
              "total": {
                "label": "Total"
              },
              "siblings": {
                "label": "Siblings"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Collapse Pagination",
            "preview": {
              "props": {
                "navLabel": "Pages",
                "prevLabel": "Previous page",
                "nextLabel": "Next page",
                "pageLabel": "Page {page}",
                "compactLabel": "{page} / {total}"
              }
            }
          }
        },
        "cardControls": ["page", "siblings", "total", "accent", "background"]
      }
    },
    {
      "name": "pagination-015",
      "type": "registry:component",
      "title": "Правка номера страницы",
      "description": "Пагинация без отдельного поля перехода: сам номер страницы становится инпутом по клику или Enter, Enter подтверждает и проверяет диапазон, а Escape отменяет правку без следа в раскладке.",
      "categories": ["pagination"],
      "dependencies": [],
      "registryDependencies": [],
      "files": [
        {
          "path": "pagination-015/pagination-015.tsx",
          "type": "registry:component",
          "target": "@components/vibeui/pagination-015.tsx"
        }
      ],
      "docs": "Компонент самодостаточен: один файл, без зависимостей, собственная палитра в локальных переменных --vibeui-pagination-015-*. Клиентский: \"use client\" ради страницы, режима правки и черновика ввода. Номер страницы обычно — <button>; клик или Enter на нём вызывает startEdit и подменяет его на <input>, который получает фокус и выделение через useEffect. Enter вызывает finishEdit(true) с проверкой диапазона и сообщением через role=\"alert\", Escape и потеря фокуса — finishEdit(false), откатывающий правку без изменения страницы.",
      "meta": {
        "tags": ["pagination", "jump", "inline edit", "form"],
        "ai": {
          "export": "Pagination015",
          "usage": "<Pagination015 page={12} total={48} />",
          "summary": "Пагинация, где номер страницы сам становится полем ввода по клику: отдельной строки с формой перехода не нужно. Enter подтверждает и проверяет диапазон с понятной ошибкой, Escape отменяет правку без следа. Ноль зависимостей, один файл, своя палитра.",
          "preserve": [
            "локальную палитру --vibeui-pagination-015-* и не подменять её токенами вашей темы (bg-input, ring-ring и подобными)",
            "перевод фокуса и выделение текста в инпуте при входе в правку — иначе редактирование начинается с пустого клика",
            "aria-invalid и role=\"alert\" у сообщения об ошибке — неверный номер должен быть объявлен, а не только подсвечен цветом",
            "откат правки по Escape и по потере фокуса без изменения текущей страницы",
            "light-dark() в палитре: компонент обязан следовать color-scheme окружения, а не носить собственную тёмную тему",
            "прозрачную подложку по умолчанию — компонент ложится на фон страницы, а не выкладывает под себя плашку"
          ],
          "adapt": [
            "page и total — начальную страницу и их общее число",
            "подписи navLabel, prevText, nextText, prevLabel, nextLabel, readoutLabel, totalLabel, inputLabel, editLabel и errorText — по умолчанию русские, язык задаёт проект",
            "акцент через проп accent — цвет кольца фокуса и подсказки-пунктира вокруг номера",
            "background — подложка компонента; пусто означает «без подложки», по цвету страницы"
          ],
          "notes": [
            "Тема берётся из color-scheme. Чтобы компонент темнел вместе с системой, у проекта должно быть объявлено color-scheme: light dark на html — иначе light-dark() всегда вернёт светлую ветку.",
            "У input type=\"number\" браузер рисует свои стрелки: если они мешают, прячьте их своим CSS, а не меняйте тип поля — числовым оно остаётся ради клавиатуры на телефоне."
          ]
        },
        "controls": [
          {
            "prop": "page",
            "label": "Страница",
            "type": "number",
            "default": 12,
            "min": 1,
            "max": 48
          },
          {
            "prop": "total",
            "label": "Всего",
            "type": "number",
            "default": 48,
            "min": 1,
            "max": 999
          },
          {
            "prop": "accent",
            "label": "Акцент",
            "type": "color",
            "default": ""
          },
          {
            "prop": "background",
            "label": "Фон",
            "type": "color",
            "default": ""
          }
        ],
        "i18n": {
          "en": {
            "description": "Pagination with no separate jump field: the page number itself turns into an input on click or Enter, Enter confirms and checks the range, and Escape cancels the edit without leaving a trace in the layout.",
            "docs": "The component is self-contained: one file, no dependencies, its own palette in the local --vibeui-pagination-015-* variables. Client component: \"use client\" for the sake of the page, the edit mode and the input draft. The page number is normally a <button>; a click or Enter on it calls startEdit and substitutes an <input>, which gets focus and its text selected through useEffect. Enter calls finishEdit(true) with a range check and a message through role=\"alert\", while Escape and losing focus call finishEdit(false), reverting the edit without changing the page.",
            "ai": {
              "summary": "Pagination where the page number itself becomes an input field on click: no separate jump-form row is needed. Enter confirms and validates the range with a clear error, Escape cancels the edit without a trace. Zero dependencies, one file, its own palette.",
              "preserve": [
                "the local --vibeui-pagination-015-* palette — do not swap it for your theme tokens (bg-input, ring-ring and the like)",
                "moving focus and selecting the text in the input when edit mode starts — otherwise editing begins from an empty click",
                "aria-invalid and role=\"alert\" on the error message — a wrong number must be announced, not only shown in color",
                "reverting the edit on Escape and on blur without changing the current page",
                "light-dark() in the palette: the component has to follow the surrounding color-scheme rather than ship a dark theme of its own",
                "the transparent background by default — the component sits on the page background instead of laying a panel under itself"
              ],
              "adapt": [
                "page and total — the starting page and their total count",
                "the navLabel, prevText, nextText, prevLabel, nextLabel, readoutLabel, totalLabel, inputLabel, editLabel and errorText captions — Russian by default, the project sets the language",
                "the accent through the accent prop — it colours the focus ring and the dashed hint around the number",
                "background — the component background; empty means no background, taking the page colour"
              ],
              "usage": "<Pagination015 page={12} total={48} />",
              "notes": [
                "The theme comes from color-scheme. For the component to darken with the system, the project must declare color-scheme: light dark on html — otherwise light-dark() always returns the light branch.",
                "The browser draws its own spinners on input type=\"number\": hide them with your own CSS if they get in the way rather than changing the field type — it stays numeric for the phone keyboard."
              ]
            },
            "controls": {
              "page": {
                "label": "Page"
              },
              "total": {
                "label": "Total"
              },
              "accent": {
                "label": "Accent"
              },
              "background": {
                "label": "Background"
              }
            },
            "title": "Inline Page Editor",
            "preview": {
              "props": {
                "navLabel": "Pages",
                "prevText": "← Back",
                "nextText": "Next →",
                "prevLabel": "Previous page",
                "nextLabel": "Next page",
                "readoutLabel": "Page",
                "totalLabel": "of {total}",
                "inputLabel": "Page number, from 1 to {total}",
                "editLabel": "Current page {page} of {total}, change the number",
                "errorText": "Enter a number from 1 to {total}"
              }
            }
          }
        },
        "cardControls": ["page", "total", "accent", "background"]
      }
    }
  ]
}
