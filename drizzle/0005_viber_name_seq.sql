-- Имя при регистрации необязательно: кто не назвался, получает «Viber000001»
-- и далее по порядку. Порядок ведёт последовательность базы, а не подсчёт
-- строк — две одновременные регистрации не получат одно имя.
CREATE SEQUENCE IF NOT EXISTS viber_name_seq START 1;
