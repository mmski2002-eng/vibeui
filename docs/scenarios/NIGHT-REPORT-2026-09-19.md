# Ночной отчёт, 19 сентября 2026

Шесть сценариев собраны, разбиты на блоки, размещены на сайте и запушены
в `main` отдельными коммитами. Все блоки — `pro`, каждый прошёл
`npm run meta:validate`, проверен глазами на 1440 и 390, в консоли чисто.

## Что сделано

| Сценарий | Slug | Коммит | Блоки | Тема |
| --- | --- | --- | --- | --- |
| Кофейня-пекарня «Корка» | `bakery` | `54674510` | 13 | светлая, мак `#e4572e`, Unbounded |
| Подкаст | `podcast` | `7946c301` | 10 | тёмная студия, лайм `#c8f542`, мини-плеер |
| Open-source проект | `opensource` | `e75eb708` | 9 | светлая, синий `#2f5bff`, терминал-хиро |
| Портфолио | `portfolio` | `76f1a209` | 8 | бумага `#efeee9`, фиолет `#5b3df5`, курсор-превью |
| Мобильное приложение «Тише» | `app` | `651729be` | 9 | лаванда `#7c5cff`, CSS-телефон, дыхание |
| AI-инструмент / SaaS «Сводка» | `saas` | `b42ca913` | 8 новых + 2 готовых | тёмная аврора `#5ee7ff`, печать по токенам |

Демо: `/scenarios/<slug>/demo`. Карточки на `/scenarios` с постерами.
Новые категории: `bakery`, `opensource`, `app` (в `registry/categories.ts`
и синонимах поиска); `ai` пополнена терминами saas / sandbox / assistant.

CI: все шесть коммитов зелёные, деплой на vibeui.ru прошёл.

## Промпты для Codex

Лежат в `docs/scenarios/<slug>/IMAGES.md`, формат как у прежних сценариев:
папка назначения, общий стиль, список файлов с промптами, «куда что идёт».

| Slug | Файлов | Сейчас на месте |
| --- | --- | --- |
| `bakery` | 25 | серые заглушки в `public/demo/bakery/` — перезаписать |
| `podcast` | 19 | заглушки в `public/demo/podcast/` — перезаписать |
| `portfolio` | 7 | заглушки в `public/demo/portfolio/` — перезаписать |
| `app` | 3 | заглушки в `public/demo/app/` — перезаписать |
| `opensource` | 0 | фото не нужны, аватары и графика — CSS; один необязательный OG |
| `saas` | 0 | фото не нужны, всё — аврора и стекло; один необязательный OG |

Имена файлов в промптах совпадают с теми, что уже лежат в `public/demo/`:
Codex просто перезаписывает webp, код не трогать.

## Открытые вопросы

Секция «Вопросы» есть только в `docs/scenarios/bakery/PLAN.md` (название
«Корка», мак vs корка, Unbounded vs антиква, обе фишки или одна). Для
остальных решения приняты по умолчанию — вот что стоит подтвердить:

- podcast: название и имя ведущей; мини-плеер докнут снизу (`docked`).
- opensource: имя пакета в терминале и цифры звёзд / загрузок — вымышленные.
- portfolio: имя и специализация героя, проекты в сетке — вымышленные.
- app: «Тише» и тема сна; QR и бейджи магазинов ведут на `#`.
- saas: «Сводка»; цены 890 / 1 490 ₽ за место; форма в CTA — заглушка.

## Что не сделано

- Линт, тайпчек и билд локально не запускались (правило) — их гонит CI.
- Мобильное меню в шапках нет: на узком ссылки скрыты, остаётся кнопка.
- Формы (subscribe, contact, cta) — заглушки, отправки нет.
