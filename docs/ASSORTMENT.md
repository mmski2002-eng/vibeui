# ASSORTMENT

Карта ассортимента: какие типы компонентов и блоков должны быть в VibeUI и
в каком порядке их делать.

Это очередь работ, а не реестр. Реестр — файлы `registry/**/registry.json`,
другого источника данных у каталога нет.

Сверка с публичным каталогом по количеству items —
[ASSORTMENT-GAP.md](ASSORTMENT-GAP.md).

## Как составлена

Ориентир по спросу — публичные каталоги shadcn-библиотек: сколько вариантов
рынок держит в каждой категории. Колонка «спрос» — порядок величины числа
вариантов у крупной публичной библиотеки (ReUI, ~1700 items). Это сигнал
приоритета, не план по количеству.

При необходимости item пишется с нуля по правилам
[PROJECT_CONTEXT.md](PROJECT_CONTEXT.md): один файл, своя палитра
`--vibeui-*`, ноль зависимостей, container-запросы, `prefers-reduced-motion`,
`meta.ai` с preserve / adapt / usage.

## Оси

Таксономия — `registry/categories.ts`. Категория компонента — это **тип**:
`tree`, `select`, `input-group`. Сборных корзин («Inputs», «Display») нет и
быть не должно: тип, спрятанный внутри корзины, невозможно найти ни в
списке категорий, ни по адресу. Директория реестра совпадает с категорией —
это проверяет `npm run indexes`.

Сейчас 80 категорий компонентов и 15 категорий блоков.

## kind: component — что осталось добрать

Состояние на 2026-09-01: **980 компонентов** против 1101 у ReUI.
Разрыв — 221 item по 34 типам, полная сверка в
[ASSORTMENT-GAP.md](ASSORTMENT-GAP.md). В 40 типах мы на их уровне или
выше, там работы нет.

Идём волнами: сначала типы, где разрыв двузначный, потом остальные. Внутри
волны — по убыванию разрыва.

### Волна 1 — 116 items, половина всего разрыва

| тип           | сейчас | цель | добрать | чего не хватает по смыслу                                                                                                    |
| ------------- | -----: | ---: | ------: | ---------------------------------------------------------------------------------------------------------------------------- |
| `input-group` |     10 |   40 |      30 | префиксы и суффиксы, кнопка внутри поля, валюта, единицы, поиск с фильтром, копирование, счётчик, загрузка, ошибка, disabled |
| `select`      |      9 |   33 |      24 | группы, иконки, аватары, описания, множественный выбор, поиск внутри, виртуальный список, статусы, страны, размеры           |
| `input`       |     15 |   31 |      16 | пароль с раскрытием, поиск, очистка, маска, автодополнение, счётчик символов, состояния (loading, success, error)            |
| `frame`       |      6 |   19 |      13 | окно браузера, телефон, планшет, ноутбук, терминал, видео, карта, коллаж, подпись, тень, наклон                              |
| `empty`       |      9 |   20 |      11 | пустой поиск, пустая папка, нет доступа, ошибка загрузки, первый запуск, фильтр не дал результата, корзина, офлайн           |
| `icon-tile`   |      6 |   17 |      11 | размеры, тона, статусы, счётчики, градиенты, обводки, группы плиток                                                          |
| `toast`       |     10 |   21 |      11 | успех, ошибка, предупреждение, загрузка, отмена действия, прогресс, стек, позиции, действие внутри                           |

### Волна 2 — 80 items

| тип             | сейчас | цель | добрать |
| --------------- | -----: | ---: | ------: |
| `radio-group`   |      8 |   17 |       9 |
| `combobox`      |     20 |   28 |       8 |
| `toggle-group`  |      8 |   16 |       8 |
| `tooltip`       |      9 |   16 |       7 |
| `dropdown-menu` |     12 |   18 |       6 |
| `pagination`    |      9 |   15 |       6 |
| `spinner`       |      6 |   12 |       6 |
| `stepper`       |      9 |   15 |       6 |
| `toggle`        |      8 |   14 |       6 |
| `label`         |      8 |   13 |       5 |
| `switch`        |      9 |   14 |       5 |
| `slider`        |      8 |   12 |       4 |
| `timeline`      |      8 |   12 |       4 |

### Волна 3 — 25 items

`chart` +3, `filters` +3, `popover` +3, `resizable` +3, `context-menu` +2,
`item` +2, `skeleton` +2, `data-grid` +1, `field` +1, `hover-card` +1,
`icon-stack` +1, `phone-input` +1, `rating` +1, `sortable` +1.

### Правила добора

- вариант должен отличаться **сценарием**, а не цветом: «поле с маской
  телефона» — да, «то же поле, но синее» — нет;
- каждый item по чек-листу из [PIPELINE.md](PIPELINE.md): один файл, своя
  палитра `--vibeui-*`, ноль зависимостей, раскладка от собственной ширины,
  `prefers-reduced-motion`, полная `meta.ai` и перевод `meta.i18n.en`;
- нумерация продолжает существующую в категории (`select-010`, `-011`, …),
  скаффолд — `npm run item -- select`;
- после каждой партии: `npm run lint`, `npm run build`, глазами страница
  категории.

## kind: block

Блоков 375 в 15 категориях — это то, чего у ReUI в каталоге компонентов нет
вовсе, и отдельный от них продукт. Текущее состояние:

| категория      | items |
| -------------- | ----: |
| `dashboard`    |    92 |
| `commerce`     |    79 |
| `solutions`    |    55 |
| `auth`         |    29 |
| `ai`           |    19 |
| `pricing`      |    19 |
| `hero`         |    16 |
| `features`     |    15 |
| `navbar`       |    13 |
| `footer`       |     8 |
| `blog`         |     6 |
| `contact`      |     6 |
| `cta`          |     6 |
| `faq`          |     6 |
| `testimonials` |     6 |

Приоритет блоков ниже компонентов: разрыв по компонентам виден при прямом
сравнении витрин, разрыв по блокам — нет.

## kind: template

Зарезервировано, реализации нет. Кандидаты — целые страницы сценариев:
биллинг, аналитика, CRM, пользователи, бронирования, файлы, инвентарь.
Раньше блоков не делать: шаблон собирается из них.

## Порядок

Волны выше — и есть очередь: `input-group`, `select`, `input`, `frame`,
`empty`, `icon-tile`, `toast`, затем вторая волна, затем третья. Внутри
типа порядок свободный, но категория закрывается целиком: недобранная
категория не догоняется сама.

Перед началом типа — сверить его счётчик у образца: каталог живой, числа
меняются. Как снимать: открыть `reui.io/components`, прочитать счётчики из
списка категорий (значения в DOM, не на картинке).
