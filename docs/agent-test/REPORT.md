# Тест доставки: 5 блоков через агента без контекста

Дата: 2026-09-15. Чистый проект `create-next-app` + `shadcn init` в scratchpad,
пять отдельных агентов (Sonnet, без доступа к репозиторию VibeUI). Каждому дана
только ссылка `/c/<name>?exp&sig&<контролы>` с прода и просьба поставить блок на
свою страницу. Значения контролов выбраны случайно и отличаются от дефолтов.

| Блок | Контролы в ссылке | Файл побайтово = реестр | Пропсы на странице | Скриншот |
| --- | --- | --- | --- | --- |
| navbar-017 | tone, brand, dateline, sales, poster, accent | да | да, но `brand` с опечаткой «Лунапарка» | navbar-017.png |
| hero-013 | tone, title, placeholder, submitLabel, accent, background | да | да, все шесть | hero-013.png |
| pricing-006 | title, defaultVolume, unitLabel, accent | да | да, все четыре | pricing-006.png |
| comparison-003 | title, eyebrow, beforeLabel, afterLabel, accent, background | да | да, все шесть | comparison-003.png |
| datagrid-018 | caption, placeholder, defaultQuery, accent | да | да, все четыре | datagrid-018.png |

Все пять агентов выполнили `npx shadcn@latest add <подписанный URL>`, никто не
переписывал код и не пересоздавал компонент. Тело установленного файла у всех
пяти совпадает с файлом реестра.

## Находки

1. **58 items с контролами без `meta.ai.export`** (navbar-017, hero-001…003,
   footer-016…018, layout-001…004, surface-*). У них `buildUsage` не может собрать
   сниппет и отдаёт авторский `ai.usage` с дефолтами, хотя в брифе написано
   «пропсы выбрал пользователь». Агент navbar-017 восстановил пропсы сам из
   query ссылки и сделал опечатку в `brand`. Лечится заполнением `export` в
   metadata.
2. **Шапка с правилами терялась у файлов с `"use client"`** (pricing-006):
   shadcn CLI прогоняет такие файлы через ts-morph и выбрасывает комментарии
   перед директивой. Исправлено в `lib/registry-docs.ts`: шапка встаёт после
   директивы.
3. Фетчер Claude Code пересказывает бриф и теряет правила-прозу (проверено до
   теста). Закрыто в этом же дне: правила списком в начале брифа, в `docs`
   registry item и в шапке файла.

Что не проверялось: Cursor, Codex, Windsurf и их фетчеры; мобильная вёрстка
установленных блоков (код тот же, что в превью).

# Сценарий «Агентство недвижимости»: сборка целой страницы из брифа

Дата: 15.09.2026. Чистый Next-проект (`agent-test`, без контекста VibeUI),
бриф `/s/realty` с подписанной ссылкой, 11 команд `npx shadcn@latest add`,
исходник страницы из брифа положен в `app/realty/page.tsx` как есть, фото —
в `public/photos/` под именами из брифа.

| Что | Результат | Скриншот |
| --- | --- | --- |
| Установка 11 блоков (realty-001…003, 005…012) | все файлы встали в `components/vibeui/`, зависимость `react-dom` у realty-003 подтянулась | — |
| Страница из брифа | рендерится 1:1 с демо `/scenarios/realty/demo`, 11 блоков в том же порядке, ошибок консоли нет | realty-agent-full.jpg / realty-demo-full.jpg |
| Яндекс Карта без ключа | iframe виджета с четырьмя метками районов | realty-map.png |
| Карточка объекта → окно | перелёт с переворотом работает в чистом проекте, Escape закрывает | realty-flip-modal.png |
| Рецепт `/scenarios/realty` | 11 блоков, экскурсия по демо, гейт Pro | realty-recipe.jpg |

Находок нет: бриф самодостаточен, агенту не пришлось ничего додумывать.
Что не проверялось: режим JS API карты с ключом (нужен ключ Яндекса), сборка
через Cursor/Codex.

# Сценарий «Ресторан»: сборка целой страницы из брифа

Дата: 15.09.2026. Чистый Next-проект (`agent-test`), бриф `/s/restaurant`
с подписанной ссылкой, 11 команд `npx shadcn@latest add`, исходник страницы
из брифа положен в `app/restaurant/page.tsx` как есть, фото — в
`public/photos/` под именами из брифа.

| Что | Результат | Скриншот |
| --- | --- | --- |
| Установка 11 блоков (restaurant-001…011) | все файлы встали, `react-dom` у галереи подтянулся | — |
| Страница из брифа | 11 блоков в порядке демо, шапка прозрачная поверх фото и темнеет при скролле, ошибок консоли нет | restaurant-agent-full.jpg / restaurant-demo-full.jpg |
| Меню | фото блюда у курсора, плашка переезжает под активный таб, смена раздела каскадом | restaurant-menu-hover.png |
| Галерея | параллакс колонок, лайтбокс с вылетом кадра открылся и в чистом проекте | restaurant-lightbox.png |
| Карта, события, хиро | Яндекс Карта в тёмной схеме с меткой, фото события по наведению, бегущая строка в первом экране | restaurant-sections.png |
| Рецепт `/scenarios/restaurant` | 11 блоков, экскурсия по демо, гейт Pro | restaurant-recipe.jpg |

Находка одна, не про наш код: Playwright при скриншоте добавляет
`caret-color:transparent` в iframe демо, React пишет hydration mismatch —
в обычном браузере предупреждения нет.

Дополнительно на всех трёх демо (фотограф, агентство, ресторан) включён
плавный скролл к якорям (`html{scroll-behavior:smooth}` с `scroll-padding-top`
под шапку) — проверено кликом по ссылкам шапки: страница едет, а не прыгает.

Перекладка по группам (15.09.2026): блоки сценариев «Агентство недвижимости»
и «Ресторан» разложены по общим категориям каталога — шапки в navbar
(`navbar-020`, `navbar-021`), первые экраны в hero (`hero-020`, `hero-021`),
формы в contact (`contact-015`, `contact-016`), агенты в team (`people-007`),
шеф в about (`about-007`), отзывы в testimonials (`testimonials-016`,
`testimonials-017`), карты в map (`map-004`, `map-005`), вопросы в faq
(`faq-016`), подвалы в footer (`footer-019`, `footer-020`), события в events
(`event-004`), плашка в cta (`cta-016`). В realty остались подборка, районы на
SVG, шаги и ипотека, в restaurant — меню и галерея. Оба брифа пересобраны в
чистом проекте под новыми именами: 22 команды, обе страницы отдают 200.
Скриншоты выше сняты до переименования, вёрстка не менялась.

# Сценарий «Онлайн-курс»: сборка целой страницы из брифа

Дата: 15.09.2026. Чистый Next-проект (`agent-test`), бриф `/s/course`,
12 команд `npx shadcn@latest add`, исходник страницы из брифа положен в
`app/course/page.tsx` как есть, фото — в `public/photos/` (пока плейсхолдеры,
промпты в `docs/scenarios/course/IMAGES.md`).

| Что | Результат | Скриншот |
| --- | --- | --- |
| Установка 12 блоков (8 в общих группах + 4 в course) | все встали, ноль зависимостей | — |
| Страница из брифа | 12 блоков в порядке демо, плавный скролл, ошибок консоли нет | course-agent-full.jpg / course-demo-full.jpg |
| «Для кого» | персоны переключаются, карточка пересобирается с каскадом | course-personas.png |
| Тарифы | переключатель рассрочки пересчитал 49 000 ₽ → 8 167 ₽/мес и в чистом проекте | course-pricing-split.png |
| Обратный отсчёт | тикает раз в секунду, на сервере рендерится нулями — hydration чистый | course-countdown.png |
| Рецепт `/scenarios/course` | 12 блоков, экскурсия, гейт Pro | course-recipe.jpg |

Находка по ходу: при создании `pricing-019` перезаписал существующий блок
(в нумерации был пропуск 017) — заметил по `git status`, восстановил из git,
свой тариф стал `pricing-020`. Правило: перед новым номером смотреть
`ls` папки, а не `count`.

## Модернизация лендинга курса (15.09.2026, вторая волна)

Переписаны все блоки сценария под современные жесты; демо проверено в
браузере на 1360 и 390 px, консоль чистая, горизонтального скролла нет.

| Блок | Что стало | Проверено |
| --- | --- | --- |
| `hero-022` | бенто: заголовок словами из размытия, четыре плитки с 3D-наклоном, mesh-фон, счётчики | course-v2-hero.jpg |
| `course-001` | три раздвигающиеся плитки с гигантским глифом | клик по второй: 225 → 670 px |
| `course-002` | закреплённая сцена: липкая колонка недель, подсветка по скроллу, минуты, иконки | course-v2-program.jpg |
| `course-003` | горизонтальная лента, drag/snap, счётчики, рисующаяся стрелка, блик | course-v2-results.jpg |
| `course-004` | календарь на семь колонок, лайв пульсирует | — |
| `about-008` | цитата во весь ряд с рисующимся маркером, бегущая строка | — |
| `pricing-020` | перелистывание цен, conic-рамка через @property, полоска мест, блик | course-v2-pricing.jpg, 49 000 → 8 167 ₽ |
| `testimonials-018` | два встречных ряда marquee, видео в `<dialog>` | dialog.open = true |
| `cta-017` | flip-часы, halo из conic-свечения | — |
| `cta-018` (новый) | плавающая кнопка с ценой: после хиро, прячется у тарифов и подвала | data-show переключается |
| `footer-021` | гигантское имя контуром, выезд по скроллу | course-v2-footer.jpg |
| демо | reveal секций через `animation-timeline: view()`, ритм фонов белый → индиго → тёмный | — |

Находки: `background` проп с градиентом ломает `color:var(--bg)` внутри
блоков (сводка недель стала чёрной на чёрном) — градиент в демо передаётся
через `style`, проп остаётся сплошным цветом. У conic-рамки `::before` с
`z-index:-1` красит всю карточку — нужна маска-кольцо (`mask-composite`).
Сборка из брифа в чистом проекте на этой волне не перегонялась.

## Сценарии «Городской фестиваль» и «Тату-студия» (16.09.2026)

Два новых сценария собраны из блоков общих групп, проверены в браузере
на 1360 и 390 px: консоль чистая, горизонтального скролла нет, интерактив
работает (фильтры, лайтбоксы, калькулятор, карусель, записи в три шага,
скроллспай навигации). Сборка из брифа в чистом проекте не перегонялась.

| Сценарий | Блоки | Особенности |
| --- | --- | --- |
| festival | navbar-023, hero-023, about-009, event-005/006/007, people-008, pricing-021, map-006, faq-018, cta-019, footer-022 | шапка-островки с кареткой, цветные капсулы в данных, фото от Codex |
| tattoo | navbar-024, hero-024, portfolio-007, people-009, process-001, stats-001, pricing-022, testimonials-019, about-010, faq-019, contact-017, cta-020, footer-023 | навигация рейкой слева и доком снизу, вывеска по буквам, всегда тёмные блоки без color-scheme на корне |

Скриншоты: tattoo-hero.jpg, tattoo-works.jpg, tattoo-pricing.jpg,
tattoo-mobile.jpg. Категории `process` и `stats` получили первые блоки —
папки заведены вручную, скаффолд `npm run item` кладёт их в components.
